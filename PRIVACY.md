# 隐私与数据处理说明

本文档描述「异界觉醒 / 转生·艾塞尔加德公会」当前版本实际处理哪些数据、怎么处理、以及**尚未闭合的风险**。它同时是给运营方的合规待办清单，不是给用户看的营销文案。

最后更新：2026-08-10

---

## 一、当前实际收集的数据

| 数据 | 用途 | 存放位置 | 留存时长 |
|---|---|---|---|
| 代号、简介 | 社区中辨识用户 | localStorage | 直到用户主动清除 |
| 角色相貌（图库路径） | 用户身份标识 | localStorage | 直到用户主动清除 |
| 职业 | 决定线下活动中的角色 | localStorage | 直到用户主动清除 |
| 真实姓名 | **可选**的信任认证 | localStorage | 直到用户主动清除 |
| 证件号**掩码**（末四位） | **可选**的信任认证，供本人辨认 | localStorage | 直到用户主动清除 |
| 证件号**原文** | 本机格式/校验位验证 | 仅存在于输入框，校验后立即清空 | 秒级，**不出设备、不入状态、不上传** |
| 精确 GPS 位置 | 计算与任务点的距离 | 浏览器内存 | 关闭页面即消失，不生成轨迹 |
| 昵称 / 简介 / 头像 URL | 展示 | 浏览器内存 | 关闭页面即消失 |

**本版本没有服务器数据库。** 服务端只有一层无状态的模型代理，不持久化任何用户数据。上表中的数据全部保存在用户自己的设备上（`localStorage` 键 `aethelgard:session:v1`），不上传、不同步。

**清除入口**：个人档案 → 清除本设备数据。这是用户撤回同意的唯一出口，任何改动都不得移除或隐藏它。

**真实姓名与证件号现在是可选的**：入门只需要一个代号。只有用户主动完成「信任认证」时才会收集，用途限定为参加需要核实身份的线下活动。

---

## 二、同意机制

首屏 `ConsentGate` 对两类数据**分别**取得同意，不打包：

- **账号与档案** —— 必选，不同意则无法进入。只涉及代号、简介与角色设定。
- **精确位置** —— 可拒绝。拒绝时任务证明无法提交，界面会明确说明原因，而不是静默失败。

**不再有生物特征这一项。** 玩家相貌改为从现成图库随机抽取，应用不再请求摄像头权限（`metadata.json` 已移除 `camera`），也不再向任何模型发送用户数据。

---

## 三、密钥处理

MiniMax API key **只存在于服务端**：

- `server/ai.ts` 通过 `Authorization: Bearer` 请求头调用 MiniMax，key 不出现在 URL 里（避免落入上游访问日志）。仅 `GroupId` 按上游要求作为查询参数附加，它是账号标识而非凭据。
- 生产由 `functions/api/*.ts`（Cloudflare Pages Functions）提供端点，key 来自平台环境变量。
- 本地由 `vite.config.ts` 的 dev 中间件提供**同一份 handler**，key 来自 `.env.local`。
- `vite.config.ts` 中已移除原先的 `define: { 'process.env.API_KEY': ... }`。**那行代码会把 key 明文编译进前端 bundle，任何访问者打开 DevTools 就能读走。**

前端代码中不存在任何 `process.env` 引用，模型厂商的 SDK 也已从前端依赖中移除——从依赖层面杜绝"顺手又在前端调一次"。

### 防止代理被当免费网关

- **来源校验**：`ALLOWED_ORIGINS` 白名单，跨站调用直接 403。
- **限流**：TTS 30 次/分钟/IP，人脸分析 10 次/5 分钟/IP。绑定 KV 时跨实例生效，否则退化为单实例内存计数。
- **人设与 schema 固定在服务端**：客户端只能传台词文本（≤300 字），无法用任意 prompt 驱动模型。
- **错误不回显**：上游错误正文只写服务端日志，客户端仅收到错误码。

---

## 四、仍未闭合的风险

以下是本轮改造**没有解决**的问题，上线前需要逐条决策。

### 4.1 到场校验仍可被绕过（高）

`ProofSubmission` 现在用真实 GPS 距离把守提交入口，替代了原先"跑完三个 setTimeout 就发奖励"的空校验。但判定发生在客户端：改过的客户端、或用系统级位置模拟工具，仍能伪造到场。

**闭合方式**：奖励发放必须移到服务端，由服务端复核位置、时间戳与照片元数据，客户端只负责上报。这需要引入后端与账号体系。

### 4.2 任务证明照片的内容完全未验证（高）

提交任务证明时上传的照片没有任何内容审核——传一张纯色图也能通过。同时也意味着**用户可能上传含他人肖像的照片**，而那些人并未同意。

（注：这里指的是任务现场证明，与角色相貌无关——相貌已改为图库随机抽取，不涉及任何拍摄。）

**闭合方式**：接入图像内容审核；在上传界面明确告知不得拍摄可识别的他人。

### 4.3 生物特征处理 —— ✅ 已彻底消除

相貌改为从现成图库随机抽取后，**面部分析链路已整个删除**：

- 前端不再有摄像头调用与画面捕获；
- `metadata.json` 已移除 `camera` 权限声明；
- `ConsentGate` 的生物特征同意项已整项移除；
- 服务端 `/api/analyze-face` 端点与图像生成调用已删除。

由此一并消除的风险：BIPA 等生物识别法规的适用、人脸数据向第三方披露、跨境传输、以及"对方数据条款存在空白"这一整类问题（曾核实：MiniMax 国际站运营方为新加坡 Nanonoble Pte. Ltd.，其公开隐私政策未指明数据存储地点、无不训练承诺、无留存期限、未提及生物识别数据）。

**同时请注意**：区域端点（如 `api-uw.minimax.io`）在官方文档中的定位是降低延迟，**不构成数据驻留承诺**。目前唯一仍会发往 MiniMax 的是语音合成的台词文本，不含个人数据；但若将来重新引入任何处理用户内容的模型调用，这条结论必须重新评估。

### 4.4 证件号收集（已缓解，但根本问题仍在）

**已缓解**：证件号与真实姓名已从首屏强制门槛降为可选的信任认证，绝大多数用户不会触碰这条路径。合规暴露面因此大幅收窄。

以下分析针对仍会走这条路径的用户，结论不变：

我们已经做到"原文不出设备、只留末四位掩码"。但更根本的问题是：**一个社区互助应用是否真的需要证件号？**

- 掩码无法用于任何实质校验，本机校验位算法也只能证明"这串数字格式合法"，不能证明"这个人是他自己"。
- 若目标是防小号，需要服务端 HMAC 哈希去重——那样原文就必须上传，风险重新出现。
- 若目标是真实身份核验，应当接入持牌 KYC 服务商，由对方承担合规责任，我方只接收"已核验"的布尔结果。

**建议**：在接入 KYC 之前，考虑直接去掉该字段，改用手机号或邮箱验证。

### 4.5 第三方图片服务 —— ✅ 已消除

全部图片已固化为 `public/assets/` 下的本地文件，**运行时不再向任何第三方图片服务发起请求**，访问者 IP 与访问时间也就不会泄漏给它们。

（仍在使用的第三方运行时资源只剩地图瓦片 CartoDB 与 CDN 上的 Tailwind / 字体。地图瓦片是功能必需；Tailwind CDN 应在正式上线前改为本地构建，它本身也会在控制台警告不适合生产环境。）

### 4.6 无账号体系 / 无数据导出通道（中）

删除路径已经有了（个人档案 → 清除本设备数据），且因为数据只在本机，删除是彻底的。

仍缺的是**数据导出**（GDPR 第 15 条、CCPA 同类要求）。当前数据量小且用户自己就能在浏览器里看到，风险有限；但一旦引入后端持久化，导出与删除都必须成为正式功能。**在设计数据库 schema 的同时就要设计这两条路径**，事后补代价高得多。

另外，本地存储意味着换设备、清浏览器数据都会丢失档案——这是无账号体系的必然代价，需要在产品上让用户预期到。

### 4.7 未成年人（中）

当前没有任何年龄门槛。若有 13 岁以下用户（美国 COPPA）或 16 岁以下（部分欧盟成员国），收集上述任何一类数据都需要监护人同意。

---

## 五、上线前检查清单

- [ ] `ALLOWED_ORIGINS` 已设置为正式域名（留空会放行 localhost）
- [ ] `MINIMAX_API_KEY` 已配置在托管平台的环境变量中，且**从未**出现在任何提交里
- [ ] `MINIMAX_BASE_URL` 与账号归属区域匹配（全球站 / 中国大陆 / 美西）
- [ ] 绑定 KV 命名空间到 `RATE_LIMIT_KV`，否则多实例部署下限流会漏算
- [ ] 隐私政策已在站内以可访问的页面形式发布（不能只放仓库里）
- [ ] 法务复核：证件号收集（现已为可选）、未成年人门槛
- [ ] 确认 `server/ai.ts` 中的模型名（`speech-2.6-hd`）在你的账号下可用
- [ ] 4.1 与 4.2 至少有一条闭合，否则奖励系统等同于无校验
